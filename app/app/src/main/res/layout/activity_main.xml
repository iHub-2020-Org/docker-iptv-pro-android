<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tv_background">

    <!-- ===== PLAY MODE ===== -->
    <FrameLayout
        android:id="@+id/playLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <SurfaceView
            android:id="@+id/surfaceView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Buffering overlay -->
        <LinearLayout
            android:id="@+id/bufferingLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="24dp"
            android:background="#CC000000"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvBuffering"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/buffering"
                android:textColor="@color/tv_text_primary"
                android:textSize="20sp" />

        </LinearLayout>

        <!-- Info bar (bottom overlay) -->
        <LinearLayout
            android:id="@+id/infoBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="horizontal"
            android:background="#CC000000"
            android:paddingHorizontal="24dp"
            android:paddingVertical="12dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvChannelIndex"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/tv_accent"
                android:textSize="18sp"
                android:layout_marginEnd="12dp"
                android:text="1" />

            <TextView
                android:id="@+id/tvChannelName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/tv_text_primary"
                android:textSize="20sp"
                android:singleLine="true"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvChannelRes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/tv_accent"
                android:textSize="16sp"
                android:layout_marginStart="16dp" />

        </LinearLayout>

    </FrameLayout>

    <!-- ===== CHANNEL LIST MODE ===== -->
    <LinearLayout
        android:id="@+id/listLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/tv_surface"
        android:visibility="gone">

        <!-- Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="20dp"
            android:background="@color/tv_background">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/channel_list_title"
                android:textColor="@color/tv_text_primary"
                android:textSize="28sp" />

            <TextView
                android:id="@+id/tvChannelCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/tv_text_secondary"
                android:textSize="20sp"
                android:text="" />

        </LinearLayout>

        <!-- Channel list -->
        <ListView
            android:id="@+id/listChannels"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:divider="@color/tv_divider"
            android:dividerHeight="1dp"
            android:scrollbarStyle="outsideOverlay"
            android:listSelector="@color/tv_list_selector" />

        <!-- Hint -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/list_hint"
            android:textColor="@color/tv_text_secondary"
            android:textSize="15sp"
            android:padding="12dp"
            android:background="@color/tv_background" />

    </LinearLayout>

    <!-- ===== SCAN MODE ===== -->
    <LinearLayout
        android:id="@+id/scanLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/tv_surface"
        android:padding="48dp"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/scan_config_title"
            android:textColor="@color/tv_text_primary"
            android:textSize="32sp"
            android:layout_marginBottom="32dp" />

        <TextView
            android:id="@+id/tvScanStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/scan_ready"
            android:textColor="@color/tv_text_secondary"
            android:textSize="20sp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/tvScanFound"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/tv_accent"
            android:textSize="18sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/scan_hint"
            android:textColor="@color/tv_text_secondary"
            android:textSize="15sp"
            android:layout_marginTop="48dp" />

    </LinearLayout>

    <!-- ===== NO-CHANNELS OVERLAY ===== -->
    <LinearLayout
        android:id="@+id/noChannelsLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tvNoChannelsTitle"
            android:text="@string/no_channels_hint"
            android:textColor="@color/tv_text_secondary"
            android:textSize="22sp"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tvNoChannelsHint"
            android:text="@string/server_hint"
            android:textColor="@color/tv_accent"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginTop="12dp" />

    </LinearLayout>

</FrameLayout>
